# Copy this file to .env and adjust values as needed.
# .env is gitignored so your local settings stay on your machine.

# Port the HTTP server listens on. Defaults to 3000 if unset.
PORT=3000

# Node environment. Set to "Development" for verbose logging via morgan.
# NODE_ENV=Development
